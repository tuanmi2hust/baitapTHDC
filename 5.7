#include <stdio.h>
#include <string.h>

int main(){
    char a[1000];
    fgets(a, 1000, stdin);
    
    
    if(a[strlen(a)] == ' '){
        a[strlen(a)] = '\0';
    }
    
    for(int i = 0; i < strlen(a); i++){
        if(a[i] == ' ' && a[i+1] == ' ' ){
            for(int j = i; j < strlen(a); j++){
                a[j] = a[j + 1];
            }i--;
        //   continue; 
        }
    //     if(a[0] == ' '){
    //     for(int i = 0; i < strlen(a); i++){
    //         a[i] = a[i+1];
    //     }
    // }
    } 
        if(a[0] == ' '){
        for(int i = 0; i < strlen(a); i++){
            a[i] = a[i+1];
        }
    }
    
    
    
        printf("%s", a);
    

    return 0;
}
